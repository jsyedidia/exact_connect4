[package]
name = "exact_connect4"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "A Rust port of Pascal Pons' exact Connect4 solver."
publish = false

[dependencies]
clap = { version = "4.5", features = ["derive"] }
rand = "0.9"

[profile.release]
lto = true
codegen-units = 1
